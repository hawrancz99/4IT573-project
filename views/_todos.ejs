<table>
    <tr>
        <th>Text</th>
        <th>Priority</th>
        <th>Done</th>
        <th>Detail</th>
    </tr>

    <% for (const todo of todos) { %>
        <tr>
            <td>
                <%= todo.text %>
            </td>
            <td>
                <% if(todo.priority==="3" ){ %>
                    <span>Very high</span>
                    <% } else if(todo.priority==="2" ){ %>
                        <span>High</span>
                        <% } else { %>
                            <span>Low</span>
                            <% } %>
            </td>
            <td>
                <%= todo.done ? 'yes' : 'no' %>
            </td>
            <td>
                <a href="/todos/detail/<%= todo.id %>">Detail</a>
                <% if (todo.done) { %>
                    <a href="/todos/toggle/<%= todo.id %>">Undone</a>
                    <a href="/todos/delete/<%= todo.id %>/false">Delete</a>
                    <% } else { %>
                        <a href="/todos/toggle/<%= todo.id %>">Done</a>
                        <% } %>
            </td>
        </tr>
        <% } %>
</table>